import {defaultStyles} from "@/constants";

export const defaultState = {
    rowState: {},
    colState: {},
    dataState: {},
    styleState: {},
    tableName: 'New Table',
    currentText: '',
    currentStyles: defaultStyles
}
